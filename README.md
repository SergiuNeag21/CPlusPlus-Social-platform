# CPlusPlus-Social-platform

This project is built in C++ and uses a custom implementation of a
linked list and multimap to store data. Users can create profiles and
send messages to connect with other users.
